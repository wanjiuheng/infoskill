# InfoSkill dependencies
# NOTE: install a torch build matching your CUDA version separately if this
# generic wheel doesn't pick up GPU support, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0
transformers>=4.43.0        # Qwen2.5-7B-Instruct (text-only) support
accelerate>=0.30.0
peft>=0.11.0
pyyaml
numpy

# ALFWorld environment (text mode: AlfredTWEnv)
alfworld
textworld[pddl]>=1.6.1
